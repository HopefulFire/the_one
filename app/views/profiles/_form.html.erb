<%= form_with profile do |profile| %>
	<%= profile.label :bio %>
	<%= profile.text_area :bio %>

	<%= profile.label :likes %>
	<%= profile.text_area :likes %>

	<%= profile.label :dislikes %>
	<%= profile.text_area :dislikes %>

	<%=  %>
<% end %>