<nav>
	<% unless logged_in? %>
		<%= link_to 'Login/Connect', new_session_path %>
		|
		<%= link_to 'Signup', new_user_path %>
	<% else %>
		<%= link_to "#{current_user.email} - Logout", session_path(current_user), method: :delete %>
	<% end %>
	|
	<%= link_to 'Home', root_path %>
</nav>