<nav>
	<% if logged_in? %>
		<%= link_to "#{current_user.nickname} - Log Out", session_path, method: :delete %>
	<% else %>
		<%= link_to 'Sign Up', new_user_path %>
		<%= link_to 'Log In', new_session_path %>
	<% end %>
</nav>